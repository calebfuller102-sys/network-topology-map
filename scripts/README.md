# Operator scripts

Backup/restore and offline image transfer scripts will be added after the API
and Compose images exist. Scripts must validate paths and preconditions rather
than silently overwriting live data.
